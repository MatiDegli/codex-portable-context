#!/usr/bin/env bash
# Transitional compatibility wrapper for the Python list CLI.

set -euo pipefail

readonly SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

source "${SCRIPT_DIR}/libcodex-python-wrapper.sh"

codex_exec_python_cli "codex_portable_context.cli.list" "$@"
